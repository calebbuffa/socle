// This module is intentionally empty.
// `Tiles3dFormat` and the `Format` trait were removed in the selekt API redesign.
// The loader factory now returns `SelectionEngineBuilder<R::Content>` directly.
