//! Codec integration layer for mesh and texture compression.
//!
//! Re-exports from `moderu` and `moderu_codec` for downstream use.

pub use moderu::append_accessor;
pub use moderu_codec::{ApplicabilityResult, CodecEncoder};
